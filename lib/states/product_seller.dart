import 'package:flutter/material.dart';
import 'package:lardgreenung/widgets/show_title.dart';

class ProductSeller extends StatelessWidget {
  const ProductSeller({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ShowTitle(title: 'จัดการสินค้า');
  }
}